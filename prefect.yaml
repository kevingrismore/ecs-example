# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: ecs-example
prefect-version: 2.14.1

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false
  - prefect_docker.deployments.steps.build_docker_image:
      id: build_image
      requires: prefect-docker>=0.3.1
      image_name: 455346737763.dkr.ecr.us-east-2.amazonaws.com/kevin-test-repo
      tag: "{{ get-commit-hash.stdout }}"
      dockerfile: Dockerfile
      platform: linux/amd64

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.3.1
      image_name: "{{ build_image.image_name }}"
      tag: "{{ build_image.tag }}"

# pull section allows you to provide instructions for cloning this project in remote locations
pull:

# the deployments section allows you to provide configuration for deploying flows
deployments:
  - name: ecs-example
    version:
    tags: []
    description:
    schedule:
    entrypoint: flow.py:hello
    parameters: {}
    work_pool:
      name: my-ecs-pool
      work_queue_name: default
      job_variables:
        image: 455346737763.dkr.ecr.us-east-2.amazonaws.com/kevin-test-repo:72ed320
    pull:
      - prefect.deployments.steps.set_working_directory:
          directory: /opt/prefect/ecs-example
